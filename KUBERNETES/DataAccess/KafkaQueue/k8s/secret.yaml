apiVersion: v1
kind: Secret
metadata:
  name: datahub-kafka-consumer-secret
  namespace: datahub1
type: Opaque
stringData:
  # Kafka is plaintext (prerequisites-kafka:9092) — no SASL credentials needed yet.
  # Add KAFKA__SASL_USERNAME / KAFKA__SASL_PASSWORD here when enabling auth.
  placeholder: ""
